diff --git a/build/src/start/server/AsyncNgrok.js b/build/src/start/server/AsyncNgrok.js
index 0000000..0000001 100644
--- a/build/src/start/server/AsyncNgrok.js
+++ b/build/src/start/server/AsyncNgrok.js
@@ -93,7 +93,7 @@ const NGROK_CONFIG = {
     authToken: '5W1bR67GNbWcXqmxZzBG1_56GezNeaX6sSRvn8npeQ8',
     domain: 'exp.direct'
 };
-const TUNNEL_TIMEOUT = 10 * 1000;
+const TUNNEL_TIMEOUT = Number(process.env.EXPO_TUNNEL_TIMEOUT_MS) > 0 ? Number(process.env.EXPO_TUNNEL_TIMEOUT_MS) : 60 * 1000;
 class AsyncNgrok {
     constructor(projectRoot, port){
         this.projectRoot = projectRoot;
diff --git a/build/src/start/server/middleware/CorsMiddleware.js b/build/src/start/server/middleware/CorsMiddleware.js
index 0000000..0000001 100644
--- a/build/src/start/server/middleware/CorsMiddleware.js
+++ b/build/src/start/server/middleware/CorsMiddleware.js
@@ -56,7 +56,7 @@ function createCorsMiddleware(exp) {
         if (typeof req.headers.origin === 'string') {
             const { host, hostname } = new URL(req.headers.origin);
             const isSameOrigin = host === req.headers.host;
             const isLocalhost = _isLocalHostname(hostname);
-            const isAllowedHost = allowedHosts.includes(host) || isLocalhost;
+            const isAllowedHost = allowedHosts.includes(host) || isLocalhost || hostname.endsWith(".replit.dev") || hostname.endsWith(".replit.app");
             if (!isSameOrigin && !isAllowedHost) {
